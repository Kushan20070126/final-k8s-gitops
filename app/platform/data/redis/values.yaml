redis:
  architecture: standalone

  image:
    registry: docker.io
    repository: bitnami/redis
    tag: "7.4.3"          
    pullPolicy: IfNotPresent

  auth:
    enabled: true
    password: "redis123"

  master:
    persistence:
      enabled: true
      size: 2Gi
      storageClass: local-path