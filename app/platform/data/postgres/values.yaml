postgresql:
  auth:
    username: appuser
    password: apppass123
    database: ceylonstaydb
    postgresPassword: postgres123

  primary:
    persistence:
      enabled: true
      size: 10Gi
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: "1"
        memory: 1Gi