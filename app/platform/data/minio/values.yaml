minio:
  auth:
    rootUser: minioadmin
    rootPassword: minioadmin123

  defaultBuckets: "uploads"

  persistence:
    enabled: true
    size: 10Gi

  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi