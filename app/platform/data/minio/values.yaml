global:
  security:
    allowInsecureImages: true

minio:
  image:
    registry: docker.io
    repository: bitnamilegacy/minio
    tag: 2024.12.18-debian-12-r1
  
  auth:
    rootUser: minioadmin
    rootPassword: minioadmin123

  defaultBuckets: "uploads"

  persistence:
    enabled: true
    size: 10Gi
    ## THIS IS THE CRITICAL LINE to match the live cluster
    storageClass: "local-path" 

  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi
