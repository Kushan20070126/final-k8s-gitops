{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "svc.fullname" . }}-secret
type: Opaque
stringData:
{{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end }}
