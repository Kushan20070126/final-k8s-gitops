apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-wrapper.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-wrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres-wrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
