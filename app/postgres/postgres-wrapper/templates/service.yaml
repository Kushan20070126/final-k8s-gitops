apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-wrapper.fullname" . }}
  labels:
    {{- include "postgres-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgres-wrapper.selectorLabels" . | nindent 4 }}
